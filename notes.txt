- PHP versions
	- if composer require complains about PHP 8 for installation of any package:
		- Windows 10:
			- search -> control panel -> search -> variables
				- Edit the system environment variables (admin access)
				- Environment Variables (bottom)
				- User Variables
					- Path
						- Edit the path for ex: php7.4.27
					- System Variables
						- Edit the path for ex: php7.4.27

			- open new terminal (open / close vs code)
				- terminal: cd K:\syncsystem\tecnologias\php\laravel\aula\laravel8\syncsystem-laravel8-v1
				- terminal: k:
			- go back to PHP 8 version - ex: php8.0.16

- Install:
	- sanctum (check if it will need)
	- passport


- run dev environment:
	- clean up and rebuild:
	TODO: maybe, put inside a composer script.
		- terminal: composer dump-autoload
		- terminal: composer dump-autoload -o

		- terminal: php artisan clear-compiled
		- terminal: php artisan optimize

		- terminal: php artisan route:clear
		- terminal: php artisan view:clear

		- terminal: php artisan cache:clear
		- terminal: php artisan config:cache
		- terminal: php artisan config:clear

	- execute symlinks defined on configuration:
	TODO: create a composer setup script.
		- update new simlinks: syncsystem-laravel8-v1\config\filesystems.php
		- terminal (root): php artisan storage:link

	- open 3 terminals in syncsystem-laravel8-v1 path
		- terminal 1 (webpack compile): npm run watch
		- terminal 2 (split): php -S localhost:8000 -t public/
			- terminal (root - composer script): start-frontend
		- terminal 3 (split): php -S localhost:8001 -t public/
			- terminal (root - composer script): start-backend

	- dev address / data:
		http://localhost:8000/admin/categories/781?masterPageSelect=layout-admin-main
		http://localhost:8000/system/categories/781?masterPageSelect=layout-admin-main
		http://localhost:8000/system/categories/edit/1825/?masterPageSelect=layout-admin-main
		http://localhost:8000/system/categories/edit/1999/?masterPageSelect=layout-admin-main
			- not so many fields with data
		http://localhost:8000/system/categories/edit/1888/?masterPageSelect=layout-admin-main
			- data in fields
		http://localhost:8000/system/
			- user: userlaravel
			- password: laravel

			- passport client:
				- http://localhost:8000/system/passport/auth/callback
				Client ID: 3
				Client secret: JwEpOFupn0DvuM2iPKa1PpT0XuXr9JCOqKVoJdVA

		http://localhost:8000/system/users/0

		http://localhost:8000/system/login-users/
			- root
			- system


			- note: when debugging secure routes, better to clear caches:
				- terminal: 
composer dump-autoload
composer dump-autoload -o
php artisan clear-compiled
php artisan optimize
php artisan route:clear
php artisan view:clear
php artisan cache:clear
php artisan config:cache
php artisan config:clear

	- custom stubs:
		- Admin Controller:
			- terminal: php artisan make:controller --type=admin AdminFiltersGenericController



- configure sanctum (production domains, test domains, etc)
	\config\sanctum.php

- Deploy:
	Update .env variables:
		CONFIG_SYSTEM_DB_TABLE_PREFIX

		